import React from 'react';
import Layout from '../components/Layout';

const LogbookPage = () => {
    return (
        <Layout>
            Logbookpage
        </Layout>
    );
};

export default LogbookPage;