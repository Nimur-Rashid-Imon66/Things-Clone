import React from 'react';
import Layout from '../components/Layout';

const UpcomingPage = () => {
    return (
        <Layout>
            UpcomingPage
        </Layout>
    );
};

export default UpcomingPage;