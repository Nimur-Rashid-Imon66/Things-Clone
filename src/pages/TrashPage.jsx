import React from 'react';
import Layout from '../components/Layout';

const TrashPage = () => {
    return (
        <Layout>
            trashpage
        </Layout>
    );
};

export default TrashPage;